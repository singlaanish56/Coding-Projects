package analysis


import (
	"github.com/singlaanish56/jsonparsergo/pkg/tokens"
)

// a lexer struct which does stores the input, the input, char, startPosition of the char,
//and the position after the current char


// a case structure to create a new token for each char, all except the number and string
// create newTokem and return

